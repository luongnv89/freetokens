/// <reference types="vite/client" />

/** Build-injected GA4 id (empty when unset/malformed). Never a VITE_ prefix. */
declare const __FT_GA_ID__: string;
/** Build-injected GoatCounter origin (empty when unset/malformed). */
declare const __FT_GC_SITE__: string;
